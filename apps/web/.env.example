# MILA frontend (apps/web). Copy to .env.local to override. All vars are build-time (VITE_).

# Data source. Default (unset) = the REAL backend via the Vite /api proxy (-> http://localhost:8000).
# Set true for offline, mock-only frontend dev (no backend needed).
# VITE_USE_MOCKS=true
