# Cycle Sentinel environment. Copy to .env and fill for LIVE mode. NEVER commit a filled .env.

# --- Inference mode: live | replay | stub  (tests use replay; the demo uses live) ---
CS_INFERENCE_MODE=replay

# --- Vultr Serverless Inference (OpenAI-compatible) ---
VULTR_INFERENCE_API_KEY=
VULTR_INFERENCE_BASE_URL=https://api.vultrinference.com/v1
# Exact ids: confirm against `GET /v1/models` (Authorization: Bearer $VULTR_INFERENCE_API_KEY).
CS_LLM_MODEL=                                               # Kimi K2 Instruct (text-only reasoning/tool LLM)
CS_RETRIEVER_MODEL=vultr/VultronRetrieverPrime-Qwen3.5-8B   # visual document retriever (Prime-8B)

# --- Protocol/SOP corpus store (page embeddings from Prime-8B), EU region — HDS-aligned ---
VECTOR_STORE=vultr        # vultr (Vultr Vector Store, EU) | pgvector (fallback)
VULTR_VECTOR_COLLECTION=  # Vultr Vector Store collection id (when VECTOR_STORE=vultr)
DATABASE_URL=postgresql+psycopg://cyclesentinel:cyclesentinel@localhost:5432/cyclesentinel  # app data (+ pgvector fallback)
VULTR_REGION=eu   # keep protocol/SOP + hormone data + inference in an EU region

# --- Vultr Object Storage (synthetic protocol/SOP source docs; private bucket only) ---
S3_ENDPOINT_URL=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=

# --- App ---
APP_SECRET_KEY=
LOG_LEVEL=INFO
