---
id: T01
wave: 0
slug: contracts
subagent: backend-core
depends_on: [T00]
owned_paths:
  - apps/api/src/loopcloser/models/__init__.py
  - apps/api/src/loopcloser/models/enums.py
  - apps/api/src/loopcloser/models/schemas.py
  - apps/api/src/loopcloser/tools/__init__.py
  - apps/api/src/loopcloser/tools/base.py
  - apps/api/src/loopcloser/agent/__init__.py
  - apps/api/src/loopcloser/agent/inference/__init__.py
  - apps/api/src/loopcloser/agent/inference/base.py
  - apps/api/src/loopcloser/agent/inference/stub.py
  - apps/api/src/loopcloser/api/__init__.py
  - apps/api/src/loopcloser/api/routes.py
  - apps/web/src/types/index.ts
interfaces_provided: [enums, schemas, Tool protocol + registry, InferenceClient protocol, StubInferenceClient, route stubs, TS types]
interfaces_consumed: []
exit_condition: "Every seam in docs/CONTRACTS.md exists and type-checks; route stubs return 501; StubInferenceClient returns canned structured outputs; TS types mirror the Python schemas."
---

# T01 — Freeze the integration seams

Implement **exactly** the interfaces in `docs/CONTRACTS.md`. These are frozen after this task — all
parallel work imports them. Do not add fields beyond the contract; if something's missing, escalate.

## Do
- `models/enums.py` — the 6-state `DecisionState` + document/candidate/validation/task enums (§1).
- `models/schemas.py` — `Citation`, `Recommendation`, `Candidate`, `Decision`, `ToolCall` (§2).
- `tools/base.py` — the `Tool` protocol + `TOOL_REGISTRY` + the 8 tool arg/result model stubs (§3).
- `agent/inference/base.py` — the `InferenceClient` protocol (§4); `stub.py` — `StubInferenceClient`
  returning deterministic canned outputs for unit tests.
- `api/routes.py` — all routes from §5 as typed stubs returning `501 Not Implemented`.
- `apps/web/src/types/index.ts` — TS mirror of enums, value objects, the SSE event union (§6), and API
  response shapes. Names identical to Python.

## DoD
- [ ] `mypy --strict` + `tsc --noEmit` clean. [ ] All 8 tool names + 6 states present verbatim.
- [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened.
