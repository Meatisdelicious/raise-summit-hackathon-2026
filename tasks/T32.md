---
id: T32
wave: 3
slug: record-cassettes
subagent: agent-orchestration
depends_on: [T30, T31]
owned_paths:
  - apps/api/tests/cassettes/
needs_credentials: true
human_supervised: true
interfaces_provided: [recorded Vultr cassettes for A–H]
interfaces_consumed: [RecordingInferenceClient, AgentRunner]
exit_condition: "Cassettes recorded from LIVE Vultr for cases A–H are committed; `make eval` in replay mode passes all gates against them; cassettes contain synthetic prompts/responses only, no secrets."
---

# T32 — Record replay cassettes from live Vultr

**Needs Vultr credentials — human-supervised** (run `/record-cassettes`). This is what makes CI
validate the same behavior the live demo produces.

## Do
- With `.env` Vultr creds set: `make record` → run the full agent over A–H with
  `RecordingInferenceClient`, writing cassettes to `apps/api/tests/cassettes/`.
- Switch to replay + `make eval`: confirm all release gates pass deterministically.
- Verify cassettes contain only synthetic data + no secrets.

## DoD
- [ ] Cassettes committed. [ ] `make eval` (replay) green against them. [ ] No secrets/real data.
- [ ] Diff within `owned_paths`. [ ] PR opened, labeled `task:T32`, noting live re-record.
