---
id: T40
wave: 4
slug: frontend-integration
subagent: frontend
depends_on: [T31, T23]
owned_paths:
  - apps/web/src/features/
  - apps/web/src/lib/
  - apps/web/src/mocks/
interfaces_provided: [live-wired web app]
interfaces_consumed: [real REST + SSE API]
exit_condition: "The web app runs the full two-minute demo path against the REAL API: open case → run → live SSE trace with a rejection branch → cited decision → approve → visible state change + audit. Citation preview opens the cited page. Mock remains as a dev fallback."
---

# T40 — Wire frontend to the real API

Swap the mock client for the real API client (same interface — this is why T13 built to the contract).

## Do
- Point the API client + SSE consumer at the real endpoints; keep the mock behind a dev flag.
- Citation preview opens the real cited page (`/documents/{id}/pages/{n}`).
- Approval flow calls the real `approve` endpoint and reflects the state change + audit.
- Manual pass of the full demo-script path in a browser.

## DoD
- [ ] Full demo path works against the real API. [ ] Citations resolve in the UI. [ ] `make verify`
  + web build green. [ ] Diff within `owned_paths`. [ ] PR opened with a screen recording/trace.
