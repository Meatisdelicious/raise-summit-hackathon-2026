---
id: T10
wave: 1
slug: synthetic-data
subagent: retrieval-data
depends_on: [T01]
owned_paths:
  - scripts/generate_synthetic_data.py
  - data/synthetic/
  - data/manifests/
  - data/templates/
interfaces_provided: [synthetic corpus, ground_truth manifest]
interfaces_consumed: [enums]
exit_condition: "Deterministic generator produces cases A–H (~30–40 docs) under data/synthetic/, each page watermarked SYNTHETIC, with data/manifests/ground_truth.json mapping every doc to expected state/evidence/citations. Every demo branch has documents + ground truth."
---

# T10 — Synthetic corpus + ground-truth manifest

Build the deterministic synthetic dataset (spec §12). **Synthetic only** — read `docs/safety.md`.

## Do
- `generate_synthetic_data.py` with a fixed seed → text-based PDFs (PyMuPDF/reportlab) under
  `data/synthetic/`. Fictional names/orgs/IDs; changed dates/values; cleared metadata; every page
  watermarked `SYNTHETIC DATA — NOT FOR CLINICAL USE`.
- The 8 ground-truth cases **A–H** (spec §12.3) — each targeting a specific state + reasoning moment:
  A `COMPLETED`, B `OPEN_OVERDUE` (panel lacks the test), C `SCHEDULED_NOT_COMPLETED`,
  D `AMBIGUOUS_REQUIRES_REVIEW` (unknown alias/conflicting identity), E `OPEN_OVERDUE` (result predates
  instruction — the signature rejection), F `COMPLETED` (valid external result), G
  `CLOSED_BY_DOCUMENTED_EXCEPTION`, H `OPEN_NOT_DUE`.
- Required document types (spec §12.4) incl. policy doc + terminology alias table.
- `data/manifests/ground_truth.json`: per case → source recommendation + citation, expected deadline,
  expected retrieval classes, valid + invalid candidates, expected state, expected action, required
  citations.

## DoD
- [ ] Regenerating with the seed is byte-stable. [ ] `make privacy` clean. [ ] Every A–H branch covered.
- [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened.
