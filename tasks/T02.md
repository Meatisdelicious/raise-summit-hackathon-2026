---
id: T02
wave: 0
slug: privacy-gate
subagent: devops-qa
depends_on: [T00]
owned_paths:
  - .gitleaks.toml
  - data/private/.gitkeep
  - scripts/privacy_scan.py
interfaces_provided: [privacy gate]
interfaces_consumed: []
exit_condition: "`make privacy` passes on a clean tree AND fails on a planted test hit; no *.PDF or data/private/ file is tracked; gitleaks config present; CI privacy job green."
---

# T02 — Phase-0 privacy gate (hard block, Phase 0 exit)

The privacy scanner (`scripts/privacy_scan.py`) and the CI privacy job **already exist** as harness
infra. This task finalizes and proves the Phase-0 gate.

## Do
- Add `.gitleaks.toml` config (health/PII + secret patterns).
- Add `data/private/.gitkeep` so the ignored dir exists but stays empty in the tracked tree.
- Extend `scripts/privacy_scan.py` patterns if needed (names/DOB/national IDs/dossier numbers/org/lab
  names/access identifiers), keeping it fast and deterministic.
- Verify the gate: it must **PASS** on the current clean tree and **FAIL** on a temporarily planted
  fixture (e.g. a fake `*.PDF` outside `data/synthetic/` or a planted identifier) — then remove the
  planted fixture.
- Confirm `git ls-files` shows no `*.PDF` and nothing under `data/private/`.

## DoD
- [ ] Gate passes clean, fails on planted hit. [ ] CI privacy job green. [ ] Diff within `owned_paths`.
- [ ] PR opened. **Nothing in Wave ≥1 may start until T00–T02 are merged.**
