---
id: T11
wave: 1
slug: db-models
subagent: backend-core
depends_on: [T01]
owned_paths:
  - apps/api/src/loopcloser/models/orm.py
  - apps/api/src/loopcloser/alembic/versions/
interfaces_provided: [ORM models, initial migration]
interfaces_consumed: [enums, schemas, db session]
exit_condition: "SQLAlchemy models for all 8 tables (spec §10) exist; a SINGLE Alembic migration creates the full schema; `make migrate` on a fresh DB succeeds; models map cleanly to the Pydantic schemas."
---

# T11 — Data model + single migration

Implement the DB layer (spec §10). **One** complete initial migration — no incremental heads (this is
what prevents parallel migration conflicts).

## Do
- `models/orm.py`: typed SQLAlchemy 2.0 (`Mapped[...]`) models for `cases`, `documents`,
  `document_chunks`, `recommendations`, `evidence_candidates`, `decisions`, `tasks`, and
  `agent_runs`/`tool_calls` — fields exactly per spec §10 and consistent with `models/schemas.py`.
- One Alembic migration under `alembic/versions/` creating the entire schema.
- Idempotent task creation support (unique constraint enabling `create_task_draft` idempotency).

## DoD
- [ ] `make migrate` on a fresh Postgres succeeds. [ ] Integration test: migrate + insert + query each
  table. [ ] `mypy --strict` clean. [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened.
