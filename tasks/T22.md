---
id: T22
wave: 2
slug: eval-harness
subagent: retrieval-data
depends_on: [T10, T12]
owned_paths:
  - scripts/eval.py
  - apps/api/tests/eval/
interfaces_provided: [release-gate eval runner]
interfaces_consumed: [ground_truth manifest, DecisionEngine, validators]
exit_condition: "`make eval` runs A–H and reports decision accuracy, false-closure count, citation resolution, task idempotency, extraction P/R, deadline accuracy, % within step limit, latency; exits non-zero if any release gate fails."
---

# T22 — Eval / release-gate harness

Spec §17. Enforces the gates that define "done" for the whole product. Runs in `replay` mode so it's
deterministic in CI. Initially can score the deterministic core against fixtures; wired to the full
agent once T30 lands (the metric contract is stable now).

## Do
- `scripts/eval.py`: load `data/manifests/ground_truth.json`; for each case A–H run the pipeline and
  compute — decision-state accuracy (**gate ≥ 90%**), **false-closure count (gate = 0)**, citation
  resolvability (**gate = 100%**), task idempotency, extraction precision/recall, deadline accuracy,
  % runs within the 12-step limit, median latency.
- Print a human-readable table + machine JSON; **exit non-zero on any gate failure**.

## DoD
- [ ] Gates computed + enforced. [ ] Deterministic across runs (replay). [ ] `make verify` green.
- [ ] Diff within `owned_paths`. [ ] PR opened.
