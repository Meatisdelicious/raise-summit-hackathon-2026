---
id: T42
wave: 4
slug: deploy
subagent: devops-qa
depends_on: [T31]
needs_credentials: true
owned_paths:
  - infra/docker/
  - infra/terraform/
  - infra/scripts/
interfaces_provided: [Vultr deployment]
interfaces_consumed: [FastAPI app, web build]
exit_condition: "The backend deploys to Vultr Compute with Vultr Managed PostgreSQL + Object Storage wired and secrets server-side; the public URL runs the LIVE agent path on synthetic data; /health and /ready pass. Frontend served/deployed and pointed at the API."
---

# T42 — Vultr deployment

Spec §8/§8.3/§20. Vultr Serverless Inference must be on the **live critical path** — hosting only the
frontend on Vultr is a weak sponsor integration. **Needs credentials.**

## Do
- `infra/docker/`: container(s) for the FastAPI app + background monitor.
- `infra/terraform/` (or documented scripts): Vultr Compute + Managed PostgreSQL + Object Storage
  (private bucket). Secrets server-side only; least-privilege; separate dev/demo creds.
- `infra/scripts/`: deploy + health-check + seed-on-deploy.
- Minimum credible integration (§8.3): backend on Vultr; planning/extraction call Vultr live; demo shows
  the model/tool events; ≥1 persistent state change stored by the deployed backend.

## DoD
- [ ] Public URL runs the live agent path on synthetic data. [ ] `/health`+`/ready` pass. [ ] No secrets
  in the repo/bundle. [ ] Diff within `owned_paths`. [ ] PR opened with the URL + a health check.
