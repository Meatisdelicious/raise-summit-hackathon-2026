---
id: T23
wave: 2
slug: frontend-features
subagent: frontend
depends_on: [T13]
owned_paths:
  - apps/web/src/features/
  - apps/web/src/components/
interfaces_provided: [inbox, workbench, timeline, trace, evidence panel, action panel]
interfaces_consumed: [TS types, mock API client]
exit_condition: "Against the mock, the full case-resolution workflow renders: inbox → workbench with timeline, live SSE trace in plain language, evidence panel (accepted/rejected + per-validator outcomes + citations + page preview), and action panel (state, draft task, approve/reject/request-review + approval audit). Accessible."
---

# T23 — Case workbench features

Spec §14. The primary experience is a **case-resolution workflow**, not a dashboard. Build against the
mock (T13); T40 wires the real API.

## Do
- **Inbox** (§14.1): filters (state/deadline/team), concise rows, one primary action (open case),
  presenter-only demo labels hidden in normal mode.
- **Timeline** (§14.2): chronological docs, distinct icons per type, click → cited page, synthetic
  watermark visible.
- **Trace** (§14.3): render the SSE event union in plain language (plan, retrievals, rejection with
  reason, expanded search, decision, drafted task). Real events, not a fabricated animation.
- **Evidence panel** (§14.4) + **Action panel** (§14.5) as specified.
- **Accessibility** (§14.6): keyboard, labels, contrast, no color-only status.

## DoD
- [ ] Full workflow renders vs mock incl. the rejection moment. [ ] `tsc` clean + a11y checks.
- [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened with a screenshot.
