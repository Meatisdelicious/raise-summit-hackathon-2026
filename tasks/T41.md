---
id: T41
wave: 4
slug: e2e-seed
subagent: devops-qa
depends_on: [T31]
owned_paths:
  - apps/web/tests/e2e/
  - apps/web/playwright.config.ts
  - scripts/seed_demo.py
  - scripts/reset_demo.py
interfaces_provided: [demo-path e2e, seed/reset]
interfaces_consumed: [real API, synthetic corpus]
exit_condition: "Playwright drives the full demo path (open→run→≥2 SSE retrievals→rejection→cited decision→approve→state mutation+audit→reset) against a seeded replay backend and passes in CI. seed_demo/reset_demo restore synthetic state idempotently."
---

# T41 — Playwright demo-path e2e + seed/reset

Spec §17.4 (e2e). Runs against a seeded backend in **replay** mode so CI is deterministic.

## Do
- `scripts/seed_demo.py` / `reset_demo.py`: idempotently load/reset the synthetic corpus + case state.
- `playwright.config.ts` + `apps/web/tests/e2e/`: the canonical demo-path spec asserting each beat of
  `docs/demo-script.md`, including the rejection branch and the persistent state mutation + audit event.

## DoD
- [ ] e2e green in CI (replay, seeded). [ ] seed/reset idempotent. [ ] `make verify`+`make e2e` green.
- [ ] Diff within `owned_paths`. [ ] PR opened.
