---
id: T13
wave: 1
slug: frontend-shell
subagent: frontend
depends_on: [T01]
owned_paths:
  - apps/web/src/app/
  - apps/web/src/lib/
  - apps/web/src/mocks/
interfaces_provided: [app shell, router, query client, mock API client]
interfaces_consumed: [TS types]
exit_condition: "The web app boots with routing + TanStack Query configured and a mock API client that implements the frozen contract (routes + SSE event union). The whole UI can be developed against the mock before the real API exists."
---

# T13 — Frontend shell + mock API client

Decouple the UI track from backend readiness (spec §14). Build against `apps/web/src/types/` +
a mock client. Use Context7 for current React/Vite/TanStack Query docs.

## Do
- `src/app/`: app shell, routing (inbox route + case-workbench route), TanStack Query provider,
  accessible layout primitives.
- `src/lib/`: a typed API client interface + SSE consumer utility matching `docs/CONTRACTS.md` §5/§6.
- `src/mocks/`: a **mock** implementation returning the A–H sample shapes and an SSE event stream (plan
  → ≥2 tool calls → candidate_rejected → retrieval_expanded → decision → task_drafted → run_complete)
  so the full workbench can be built now.

## DoD
- [ ] `tsc --noEmit` clean, `npm run build` succeeds. [ ] App boots and navigates inbox ↔ workbench
  against the mock. [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened.
