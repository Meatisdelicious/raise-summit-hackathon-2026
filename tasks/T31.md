---
id: T31
wave: 3
slug: api-endpoints
subagent: backend-core
depends_on: [T11, T21, T30]
owned_paths:
  - apps/api/src/loopcloser/api/cases.py
  - apps/api/src/loopcloser/api/runs.py
  - apps/api/src/loopcloser/api/tasks.py
  - apps/api/src/loopcloser/api/demo.py
  - apps/api/tests/e2e_api/
interfaces_provided: [full REST + SSE API]
interfaces_consumed: [AgentRunner, tools, ORM models]
exit_condition: "All routes in docs/CONTRACTS.md §5 are implemented (replacing the 501 stubs), including the SSE /events stream, task approve/reject/request-review with real state mutation + audit, and a protected /demo/reset that restores only synthetic state. TestClient e2e passes in replay mode."
---

# T31 — API endpoints

Spec §11. Turn the frozen route stubs into working endpoints wired to the orchestrator + tools.

## Do
- `cases.py`: cases list/detail, documents, page fetch.
- `runs.py`: `POST /cases/{id}/runs` (start a run), `GET /runs/{id}`, `GET /runs/{id}/events` (SSE via
  sse-starlette streaming the orchestrator's events).
- `tasks.py`: approve (authenticated → state mutation + audit), reject, request-review.
- `demo.py`: protected `/demo/reset` (synthetic state only) + wire `/health`/`/ready`.
- e2e (TestClient, replay): open case → run → stream events → approve → assert persistent state change +
  audit event → reset.

## DoD
- [ ] All §5 routes live (no 501s). [ ] SSE streams the run. [ ] Approve mutates state + audits.
- [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened.
