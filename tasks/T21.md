---
id: T21
wave: 2
slug: tool-registry
subagent: backend-core
depends_on: [T11, T12]
owned_paths:
  - apps/api/src/loopcloser/tools/impl/
  - apps/api/tests/unit/test_tools.py
interfaces_provided: [8 registered tools]
interfaces_consumed: [Tool protocol, validators, retrieval interface, ORM models]
exit_condition: "All 8 tools are implemented, registered, and Pydantic-validated. create_task_draft is idempotent; approve_task requires an authenticated approver and mutates state + writes audit; the model cannot call an unregistered tool."
---

# T21 — The 8-tool registry

Implement the eight tools (spec §7) over the deterministic core (T12) + retrieval interface (T20) +
ORM (T11). Consume the retrieval **interface**, not its internals.

## Do
- `tools/impl/`: `search_documents`, `get_document_context`, `resolve_deadline`, `normalize_target`,
  `validate_completion` (returns per-check dict + pass/fail/ambiguous), `create_task_draft`
  (**idempotent** — repeated runs don't duplicate), `approve_task` (authenticated; records approver +
  new state + audit event), `schedule_monitor`.
- Register all into `TOOL_REGISTRY`; validate args/results with the Pydantic models from the seam.
- Unit tests incl. idempotency of `create_task_draft` and the auth requirement of `approve_task`.

## DoD
- [ ] 8 tools registered + validated. [ ] Idempotency + auth tested. [ ] Unregistered-tool call rejected.
- [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened.
