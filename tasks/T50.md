---
id: T50
wave: 5
slug: demo-harden
subagent: devops-qa
depends_on: [T40, T41, T42, T32]
owned_paths:
  - scripts/demo_runs.py
  - scripts/measure_latency.py
  - infra/scripts/
interfaces_provided: [demo reliability + fallback]
interfaces_consumed: [deployed app, e2e]
exit_condition: "Primary + backup cases pass FIVE consecutive end-to-end runs; latency measured; the transparent last-successful 'recorded run' fallback works and is clearly labeled (never faked as live); the demo dataset is frozen."
---

# T50 — Demo hardening

Spec §7 phase 7, §15 backup, §18. The demo is 50% of judging — protect it.

## Do
- `scripts/demo_runs.py`: run the primary + backup cases end-to-end **5 consecutive times**; fail if any
  run deviates.
- `scripts/measure_latency.py`: capture per-inference + per-tool + end-to-end latency.
- Implement the transparent fallback: preserve the last successful synthetic run; if live inference
  fails during the demo, show it **clearly labeled as a recorded run**.
- Freeze the demo dataset (pin the synthetic corpus + cassettes).

## DoD
- [ ] 5/5 consecutive passes on primary+backup. [ ] Latency report produced. [ ] Fallback labeled +
  working. [ ] `make verify` green. [ ] Diff within `owned_paths`. [ ] PR opened.
