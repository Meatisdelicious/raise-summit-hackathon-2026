---
id: T00
wave: 0
slug: scaffold
subagent: devops-qa
depends_on: []
owned_paths:
  - apps/api/pyproject.toml
  - apps/api/src/loopcloser/__init__.py
  - apps/api/src/loopcloser/main.py
  - apps/api/src/loopcloser/config.py
  - apps/api/src/loopcloser/db.py
  - apps/api/src/loopcloser/alembic.ini
  - apps/api/src/loopcloser/alembic/env.py
  - apps/api/src/loopcloser/alembic/script.py.mako
  - apps/web/package.json
  - apps/web/package-lock.json
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - ruff.toml
  - mypy.ini
interfaces_provided: [FastAPI app, /health, /ready, settings, db session]
interfaces_consumed: []
exit_condition: "`make install` succeeds; `uvicorn loopcloser.main:app` serves /health and /ready; `npm run build` builds the empty web app; all Python + JS deps are pinned."
---

# T00 — Repo code scaffold + all dependencies

Create the monorepo code skeleton. **This is the only task allowed to create shared manifests**
(`pyproject.toml`, `package.json`) — front-load **every** dependency any later task will need so no
later task edits a manifest (escalate if one is missing).

Note: the harness files (`Makefile`, `.github/**`, `scripts/privacy_scan.py`,
`scripts/check_ownership.py`, `.env.example`, `.gitignore`, PR template, `docs/**`, `AGENTS.md`,
`CLAUDE.md`, `.claude/**`) **already exist** — do not recreate them.

## Do
- `apps/api/pyproject.toml`: Python 3.12, deps = fastapi, uvicorn[standard], pydantic v2,
  pydantic-settings, sqlalchemy 2.x, alembic, psycopg[binary], boto3 (or minio), pymupdf, pypdf,
  httpx, sse-starlette, pytest, pytest-asyncio, ruff, mypy. Configure pytest + package layout
  (`src/loopcloser`).
- `main.py`: FastAPI app exposing `GET /api/health` and `GET /api/ready`.
- `config.py`: pydantic-settings reading the `.env.example` variables (incl.
  `LOOPCLOSER_INFERENCE_MODE`).
- `db.py`: SQLAlchemy 2.0 engine + session factory from `DATABASE_URL`.
- Alembic init (env + ini + mako) — **no migration yet** (T11 creates the single migration).
- `apps/web`: Vite + React + TS skeleton with all JS deps pinned (react, react-dom,
  @tanstack/react-query, a router, playwright as dev dep). Empty `App.tsx`.

## DoD
- [ ] `make install` succeeds. [ ] App serves `/health`+`/ready`. [ ] `npm run build` succeeds.
- [ ] `make verify` green (subset — no product code yet). [ ] Diff within `owned_paths`. [ ] PR opened.
